---
apiVersion: v1
kind: Service
metadata:
  name: nginx-proxy
  namespace: {{ third_parties_namespace }}
spec:
  selector:
    app: nginx-proxy
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http-port
