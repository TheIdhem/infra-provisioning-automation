api_version: networking.k8s.io/v1
kind: Ingress
metadata:
  name: storage-service
  namespace: "{{ storage_service_namespace }}"
  annotations: 
      kubernetes.io/ingress.class: nginx
      nginx.ingress.kubernetes.io/auth-tls-verify-client: "optional"
      nginx.ingress.kubernetes.io/auth-tls-secret: "{{ storage_service_namespace }}/unagi-ca-crt"
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
      external-dns.alpha.kubernetes.io/hostname: api.storage.{{ public_domain }}
      nginx.ingress.kubernetes.io/global-rate-limit: "600"
      nginx.ingress.kubernetes.io/global-rate-limit-window: "1m"
      nginx.ingress.kubernetes.io/auth-tls-pass-certificate-to-upstream: "true"
spec:
  tls:
  - hosts:
    - "api.storage.{{ public_domain }}"
    secretName: storage-tls
  rules:
    - host: "api.storage.{{ public_domain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "storage-service"
                port:
                  number: 80
